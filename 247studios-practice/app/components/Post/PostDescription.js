import styled from 'styled-components';

const PostDescription= styled.div`
    display: flow-root;
    position: relative;
    padding: 8px 16px;
    padding-left: 0;  
    position:relative;
    top: 0;  
`;

export default PostDescription;
