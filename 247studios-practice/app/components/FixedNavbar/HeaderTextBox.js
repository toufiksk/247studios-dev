import styled from 'styled-components';

const HeaderTextBox = styled.div`
    height: 48px;
    width: calc(55px + 4px * 2);
    float: left;
    vertical-align: bottom;
    padding-top: 8px;
    padding-left: 4px;
    padding-right: 4px;

`;

export default HeaderTextBox;
